{
    {% for plan in plans %}
        "{{ plan.date|capitalize }}": [
                {% for aliment in plan.aliments %}
                {
                    "hour": "{{ aliment.hour }}",
                    "aliment": "{{ aliment.name }}",
                    "quantity": "{{ aliment.quantity }}",
                    "is_eaten": "{{ aliment.iseaten }}",
                    "is_model": "{{ aliment.ismodel }}"
                }
                {% if loop.last == false %},
                {% endif %}
                {% endfor %}
            ]

        {% if loop.last == false %},
        {% endif %}
    {% endfor %}
}
